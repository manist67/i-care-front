import { } from '../actions';

const INITIAL_STATE = {
    token: null,
    seq: null,
    name: null,
}

function UserReducer( state = INITIAL_STATE, action ) {
    switch(action.type) {
        default:
            break;
    }

    return state;
}

export default UserReducer;